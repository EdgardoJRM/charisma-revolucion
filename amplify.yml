version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - echo "Frontend files are already in public/"
        - ls -la public/
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
backend:
  phases:
    build:
      commands:
        - cd amplify/backend/function/evaluarCharisma/src
        - npm install
  artifacts:
    baseDirectory: amplify/backend
    files:
      - '**/*'
    discardPaths: no
appRoot: .
