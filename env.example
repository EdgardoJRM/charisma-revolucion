# Configuración del servidor
PORT=3000

# Configuración de AWS SES
# Necesitas tener configuradas tus credenciales de AWS
# y un dominio/email verificado en SES
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=tu-access-key-id
AWS_SECRET_ACCESS_KEY=tu-secret-access-key
AWS_SES_FROM_EMAIL=noreply@tudominio.com

# Configuración de DynamoDB
# Crea una tabla en DynamoDB con:
# - Partition key: id (String)
# - No necesitas sort key
DYNAMODB_TABLE=charisma-revolucion-clientes

# Notas:
# 1. El email en AWS_SES_FROM_EMAIL debe estar verificado en SES
# 2. Si estás en el sandbox de SES, solo puedes enviar a emails verificados
# 3. Para producción, solicita salir del sandbox en la consola de AWS
# 4. Crea la tabla DynamoDB antes de usar el sistema
